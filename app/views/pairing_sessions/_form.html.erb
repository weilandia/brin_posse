<%= form_for(@pairing_session) do |f| %>
  <% if @pairing_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pairing_session.errors.count, "error") %> prohibited this pairing_session from being saved:</h2>

      <ul>
      <% @pairing_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :complete %><br>
    <%= f.number_field :complete %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
